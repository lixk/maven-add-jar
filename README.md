# maven-add-jar
本程序可以实现对指定目录的监控，在目录内新增或者修改jar文件，都会触发maven自动添加变动的jar文件到maven仓库

### 使用说明
直接运行`main.py`文件，windows系统下可以直接运行build目录下打包好的二进制文件（不需python环境）。程序首次运行会创建`my.ini`配置文件，用于配置maven安装路径和扫描监控的目录，在该文件中配置好对应信息之后再次启动程序即可。  
此时在监控目录中添加或者修改jar文件即会自动将变动的jar文件添加到maven仓库
